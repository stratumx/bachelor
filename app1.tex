\chapter{Программа генерации архимедовой сферической спирали}
\label{app:ash}

\begin{lstlisting}[
    language=python,showstringspaces=false,basicstyle=\ttfamily\small,
    numbers=left,numberstyle=\footnotesize,numbersep=5pt,frame=tb
]
import math
from pprint import pprint

import matplotlib.pyplot as plt
import numpy as np
from mpl_toolkits.mplot3d import Axes3D

# входные данные
LEANING_ANGLE = 0	# угол положения
RADIUS = 70		# радиус
ROTATION_COUNT = 17	# число витков
FINENESS = 0.25		# шаг

# расчёт вертикального и горизонтального углов
v_angle_unit = math.pi / RADIUS / 2
h_angle_unit = math.pi / RADIUS * ROTATION_COUNT * FINENESS

xr = LEANING_ANGLE / 180 * math.pi
xrc = math.cos(xr)
xrs = math.sin(xr)

pnts = []
total_rot = 0
flg = -1

# генерация точек кривой
i = -RADIUS
while i <= RADIUS:
  x = math.cos(i * v_angle_unit) * RADIUS
  y = math.sin(i * v_angle_unit) * RADIUS
  
  v = [x*math.cos(total_rot), y, x*math.sin(total_rot)]
  
  pnts.append([v[0], v[1] * xrc - v[2] * xrs, v[2] * xrc + v[1] * xrs])
  total_rot += h_angle_unit
  i += FINENESS

# процедура формирования файла с координатами
n = len(pnts)
coords = open('./line-'+str(RADIUS)+'-'+str(ROTATION_COUNT)+'.sldcrv','w')
for i in range(n):
  xr = round(pnts[i][2],3)
  yr = round(pnts[i][1],3)
  zr = round(pnts[i][0],3)
  coords.write(str(xr)+' '+str(yr)+' '+str(zr)+'\n')
coords.close()

# вывод графика
fig = plt.figure()
ax = fig.gca(projection='3d')

X = np.zeros(n)
Y = np.zeros(n)
Z = np.zeros(n)

for i in range(n):
  for j in range(3):
    if j == 0:
      Z[i] = pnts[i][j]
    elif j == 1:
      Y[i] = pnts[i][j]
    else:
      X[i] = pnts[i][j]

ax.plot(X, Y, Z, label='curve')
 
ax.set_xlabel(r'x')
ax.set_ylabel(r'y')
ax.set_zlabel(r'z')

fig.savefig("Test.svg",bbox_inches="tight",pad_inches=.15)

plt.show()

# процедура группировки координат
def makePathAndPutIntoGroup(pnts, group):
  if pnts.length > 0:
    if(pnts.length > 1):
      pnts = [pnts.pop(), pnts.pop()].reverse()
    else:
      pnts = [pnts.pop()]
  return pnts

\end{lstlisting}